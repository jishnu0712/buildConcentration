import { StyleSheet } from 'react-native'

const ExitComponentsStyle = StyleSheet.create({
    crossIcon: {
      width: 20,
      height: 20,
    },
    exitView: {
      alignItems: 'flex-end',
      margin: 10,
    },
    
  });

export default ExitComponentsStyle